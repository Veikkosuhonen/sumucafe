<!DOCTYPE html>
<html lang="fi">
  <head>
    <title>Sumucafe</title>
    <meta name="description" content="Sumucafe: better menu for Unicafe">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-stone-200 flex flex-col min-h-[100vh]">

    <section class="mb-8 p-4 flex items-center backdrop-blur-sm rounded-xl">
      <%= link_to "SUMUCAFE", root_path, class: "text-sm font-bold p-4 mr-2 text-stone-700 hover:bg-stone-100 transition-colors ease-in-out duration-300 rounded-lg" %>
      <span class="text-sm text-stone-500 hidden md:block ml-auto">- Vastaukset päivän polttaviin kysymyksiin klo 10.55-10.57</span>
    </section>

    <div class="p-4 mb-8 flex flex-col md:flex-row gap-x-4 gap-y-16">
      <aside class="flex flex-col p-2 md:p-4 gap-4 bg-stone-500 rounded-xl" >
        <% if current_user %>
          <span class="text-xs text-stone-300">Hei <%= current_user.username %></span>
          <%= link_to 'Oma sivu', current_user, class: "text-stone-200 text-sm" %>
          <%= link_to 'Kirjaudu ulos', '/logout', class: "text-stone-200 text-sm" %>
        <% else %>
          <%= link_to 'Login', '/login', class: "text-stone-200 text-sm" %>
          <%= link_to 'Sign up', 'signup', class: "text-stone-200 text-sm" %>
        <% end %>
      </aside>

      <main class="container mx-auto flex">
        <%= yield %>
      </main>
    </div>

    <%= render "partials/footer" %>
  </body>
</html>
