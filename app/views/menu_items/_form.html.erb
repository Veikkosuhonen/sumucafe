<%= form_with(model: menu_item) do |form| %>
  <% if menu_item.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(menu_item.errors.count, "error") %> prohibited this menu_item from being saved:</h2>

      <ul>
        <% menu_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :restaurant_id, style: "display: block" %>
    <%= form.number_field :restaurant_id %>
  </div>

  <div>
    <%= form.label :meal_id, style: "display: block" %>
    <%= form.number_field :meal_id %>
  </div>

  <div>
    <%= form.label :menu_date, style: "display: block" %>
    <%= form.datetime_field :menu_date %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
